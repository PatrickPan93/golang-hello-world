apiVersion: v1
kind: Service
metadata:
  name: {{ include "golang-hello-world.fullname" . }}
  labels:
    {{- include "golang-hello-world.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "golang-hello-world.selectorLabels" . | nindent 4 }}
